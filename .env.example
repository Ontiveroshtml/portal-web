# Copiar a .env. En producción se compila con los valores reales.

# API del backend (con /api al final).
VITE_API_BASE_URL=http://localhost:4000/api

# URL de la app de gestión. Si falta se usa https://app.guildcore.pro.
VITE_APP_URL=http://localhost:5173
